services:
  - type: web
    name: flipkart-scraper
    env: node
    plan: free
    buildCommand: |
      npm install
      npx playwright install chromium
    startCommand: npm start
    autoDeploy: true
